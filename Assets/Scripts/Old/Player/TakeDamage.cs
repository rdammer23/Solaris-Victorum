/*using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets.Scripts.Player
{
    class TakeDamage
    {
        PlayerHealth _playerHealthChange;

        public void ShipCollision()
        {
            int healthChange = -7;
            _playerHealthChange = new PlayerHealth();
            _playerHealthChange.HealthChange(healthChange);           
        }

        public void LaserHit()
        {
            int healthChange = -20;
            _playerHealthChange = new PlayerHealth();
            _playerHealthChange.HealthChange(healthChange);
        }
    }
}
*/